spring:
  datasource:
    url: jdbc:h2:mem:todo_test;MODE=MySQL;DB_CLOSE_DELAY=-1;DATABASE_TO_LOWER=TRUE;INIT=RUNSCRIPT FROM 'classpath:schema.sql'
    driver-class-name: org.h2.Driver
    username: sa
    password:
  sql:
    init:
      mode: never
  mail:
    host: localhost
    port: 25
    username: test@example.com
    password: test

mybatis-plus:
  configuration:
    map-underscore-to-camel-case: true

jwt:
  secret: FabTodoSecretKeyForJWT2024MustBeAtLeast256BitsLong!@#$%^
  expiration: 86400000
